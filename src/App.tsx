import { type FC } from "react";
import { Toaster } from "react-hot-toast";

const App: FC = () => {
    return (
        <>
            <h1>Hello World!</h1>
            <Toaster />
        </>
    );
};

export default App;
